# Legacy STRIDE Prompt Redirect

STRIDE has been renamed to COMPASS.

Use `prompts/compass-analysis.md` as the canonical prompt.

During migration, interpret this prompt as a COMPASS launcher and apply the active COMPASS rule files.
