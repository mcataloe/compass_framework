# Migration Notes

Scaffold placeholder. Existing migration notes override this template.

This folder stores Source Rebase reports and migration notes.

Reports are evidence of scaffold alignment, not claim verification.

Do not rewrite historical checkpoints or normalize legacy filenames such as `COMPASS_Layer0_*`.
